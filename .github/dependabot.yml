# Keeps GitHub Actions up to date. Because the workflow pins actions to commit
# SHAs, Dependabot opens a PR when a newer release exists and updates both the
# SHA and the trailing "# vX" comment. Weekly is plenty for this small repo.
version: 2
updates:
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
